import{_ as e,a as t}from"./chunks/centos_sw_selection.0WNxScw8.js";import{_ as o,o as r,c as a,R as n}from"./chunks/framework.O0REyYVi.js";const w=JSON.parse('{"title":"在 CentOS 7 上使用 QGC","description":"","frontmatter":{},"headers":[],"relativePath":"zh/qgc-dev-guide/getting_started/CentOS.md","filePath":"zh/qgc-dev-guide/getting_started/CentOS.md"}'),l={name:"zh/qgc-dev-guide/getting_started/CentOS.md"},i=n('<h1 id="在-centos-7-上使用-qgc" tabindex="-1">在 CentOS 7 上使用 QGC <a class="header-anchor" href="#在-centos-7-上使用-qgc" aria-label="Permalink to &quot;在 CentOS 7 上使用 QGC&quot;">​</a></h1><h2 id="安装操作系统" tabindex="-1">安装操作系统 <a class="header-anchor" href="#安装操作系统" aria-label="Permalink to &quot;安装操作系统&quot;">​</a></h2><p>安装CentOS 7</p><ol><li>从这里获取最新<a href="http://isoredirect.centos.org/centos/7/isos/x86_64/CentOS-7-x86_64-DVD-1810.iso" target="_blank" rel="noreferrer">CentOS 7 ISO </a></li><li>通过<a href="https://linuxize.com/post/how-to-create-a-bootable-centos-7-usb-stick/" target="_blank" rel="noreferrer">following this guide</a>制作一个 USB 启动盘。</li><li>从USB启动盘中启动目标设备。</li></ol><p>下面的示例将说明如何从USB启动盘启动目标设备。</p><p><strong>Example：</strong> <em>Panasonic Toughpad FZ-M1</em> （为方便遵循本指南操作，建议在设备上附上一个键盘和鼠标）。</p><ol><li>在附加的 USB 键盘上按住 <strong>Delete</strong> 键，或者在启动时按下电源按钮周围的所有按钮，便可进入BIOS 菜单。</li><li>在 BIOS 菜单界面使用箭头键或触摸屏切换到 <em>Exit</em> 选项卡。</li><li>从启动设备列表中，选择已插入的USB启动盘。</li></ol><p><strong>Example</strong> <a href="https://www.uavcomp.com/command-control/micronav/" target="_blank" rel="noreferrer">UAV 组件微导航</a> 设备：</p><ol><li>CentOS 不会以默认配置安装。 为解决这个问题，可进行如下操作： <ol><li>如上述的示例中所述，转到BIOS菜单。</li><li>在“Advanced”选项卡下禁用“Extension Port”设备。</li><li>在BIOS菜单的退出页面上选择“Exit and save”，然后从USB启动盘启动目标设备。</li><li>在 CentOS 安装成功后，可再次恢复更改，以便微硬网络正常工作。</li></ol></li><li>如果想要重新进入Linux操作系统，请先关闭设备，而不是热重启。 否则微硬网络适配器将无法正常工作并怠速整个系统。</li></ol><h3 id="centos-软件选择安装选项" tabindex="-1">CentOS 软件选择安装选项 <a class="header-anchor" href="#centos-软件选择安装选项" aria-label="Permalink to &quot;CentOS 软件选择安装选项&quot;">​</a></h3><p>这些都是用于安装CentOS开发系统的选项。 使用它作为指南。</p><p><img src="'+e+'" alt="安装操作系统../Qt/5.12.4"></p><p><img src="'+t+`" alt="CentOS 软件选项"></p><h3 id="更新-gstreamer" tabindex="-1">更新 GStreamer <a class="header-anchor" href="#更新-gstreamer" aria-label="Permalink to &quot;更新 GStreamer&quot;">​</a></h3><p>CentOS 安装并启动后，我们需要为QGC 搭建环境。 首先，我们需要将GStreamer更新为最新版本。 本指南遵循Alice Wonder的提示：<a href="https://media.librelamp.com" target="_blank" rel="noreferrer">https://media.librelamp.com</a>。</p><pre><code>sudo yum install epel-release -y
wget http://awel.domblogger.net/7/media/x86_64/awel-media-release-7-6.noarch.rpm
sudo yum localinstall awel-media-release-7-6.noarch.rpm -y
sudo yum clean all
sudo yum update
sudo yum install gstreamer1* --skip-broken -y
</code></pre><p>**Note:**确保如下组件已安装 (Intel GPUs的 vaapi)</p><pre><code>sudo yum install gstreamer1-vaapi
sudo yum install gstreamer1-libav
</code></pre><p><strong>Note:</strong> Install these to enable hardware accelerated video decoding</p><pre><code>sudo yum install libva
sudo yum install libva-utils
sudo yum install libva-intel-driver
</code></pre><p>If libva-intel-driver is not found you can download it and install it manually</p><pre><code>wget http://download1.rpmfusion.org/free/el/updates/7/x86_64/l/libva-intel-driver-1.8.3-4.el7.x86_64.rpm
sudo yum localinstall libva-intel-driver-1.8.3-4.el7.x86_64.rpm -y
</code></pre><h3 id="安装sdl2" tabindex="-1">安装SDL2 <a class="header-anchor" href="#安装sdl2" aria-label="Permalink to &quot;安装SDL2&quot;">​</a></h3><p>SDL2 is used for joystick support.</p><pre><code>sudo yum install SDL2 SDL2-devel -y
</code></pre><h3 id="更新内核-可选" tabindex="-1">更新内核(可选) <a class="header-anchor" href="#更新内核-可选" aria-label="Permalink to &quot;更新内核(可选)&quot;">​</a></h3><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>当运行命令<code>/dev/input/*</code> 时， 如果游戏杆被成功识别并显示为<code>/dev/input/js0</code> ，则可以跳过这一步。</p></div><p>We recommend updating the kernel for:</p><ul><li>更好地触摸屏幕响应性能。</li><li>正确识别某些USB设备 - 特别是游戏杆。</li></ul><p>The following joysticks are known not do work out of the box with the default CentOS 7 kernel (3.10.0):</p><ul><li>Logitech F310</li><li>Microsoft Xbox 360 controller (USB)</li></ul><p>To fix the joystick not being recognized (even if the same unit is working under Windows or Ubuntu) please <a href="https://www.howtoforge.com/tutorial/how-to-upgrade-kernel-in-centos-7-server/" target="_blank" rel="noreferrer">follow this guide to update the kernel</a>.</p><p>Here&#39;s a short summary of the commands that you need to execute to update the kernel:</p><pre><code>sudo rpm --import https://www.elrepo.org/RPM-GPG-KEY-elrepo.org
sudo rpm -Uvh http://www.elrepo.org/elrepo-release-7.0-2.el7.elrepo.noarch.rpm
sudo yum --enablerepo=elrepo-kernel install kernel-ml -y
</code></pre><p>Reboot your device afterwards and make sure the new kernel version shows up as the default start option in the GRUB menu on boot.</p><div class="info custom-block"><p class="custom-block-title">INFO</p><p>您可能需要在 BIOS 中禁用安全启动才能启动新内核。</p></div><h2 id="在-centos-上运行-qgc" tabindex="-1">在 CentOS 上运行 QGC <a class="header-anchor" href="#在-centos-上运行-qgc" aria-label="Permalink to &quot;在 CentOS 上运行 QGC&quot;">​</a></h2><p>Before launching QGC, you need to make sure the current user has access to the dialout group (serial port access permission):</p><pre><code>sudo usermod -a -G dialout $USER
</code></pre><h3 id="防火墙" tabindex="-1">防火墙 <a class="header-anchor" href="#防火墙" aria-label="Permalink to &quot;防火墙&quot;">​</a></h3><p>The default firewall security level of Red Hat distributions like CentOS block MAVLink communication and also the camera video stream. So you need to create rules to open the incoming ports for MAVLink and camera stream. For non-production local environment testing purposes ONLY you can temporarily disable the firewall using the following commands (<a href="https://www.liquidweb.com/kb/how-to-stop-and-disable-firewalld-on-centos-7/" target="_blank" rel="noreferrer">from here</a>):</p><p>Temporary:</p><pre><code>systemctl stop firewalld
</code></pre><p>Permanent (at your own risk):</p><pre><code>systemctl disable firewalld
</code></pre><p>Undo permanent change:</p><pre><code>systemctl enable firewalld
</code></pre><h3 id="与多网络的连接问题" tabindex="-1">与多网络的连接问题 <a class="header-anchor" href="#与多网络的连接问题" aria-label="Permalink to &quot;与多网络的连接问题&quot;">​</a></h3><p>In our test with CentOS we had problems when connecting to multiple networks through multiple network devices even with appropriate IP address and subnet assignment.</p><p>Issues consisted of:</p><ul><li>连接到第二个网络时失去互联网连接</li><li>与载具的连接质量不佳，存在有大量不确定非再现问题和包丢失（例如，在常规模式中， 30秒完美连接，4秒丢失数据包)</li></ul><p>If you face any of these problems avoid them by only connecting one network at a time e.g. switching between WiFi and Microhard.</p><h3 id="执行预构建的-qgc-二进制文件" tabindex="-1">执行预构建的 QGC 二进制文件 <a class="header-anchor" href="#执行预构建的-qgc-二进制文件" aria-label="Permalink to &quot;执行预构建的 QGC 二进制文件&quot;">​</a></h3><ul><li><p>为CentOS保留包含预构建的QGC二进制文件的归档。 目前，这个构建没有自动部署，如果您迫切需要，请与开发者取得联系，。</p></li><li><p><a href="https://www.hostdime.com/kb/hd/command-line/how-to-tar-untar-and-zip-files" target="_blank" rel="noreferrer">解压归档</a></p></li><li><p>从解压文件里找到脚本文件<code>qgroundcontrol-run.sh</code></p></li><li><p>输入如下命令，运行该脚本</p><pre><code>./qgroundcontrol-run.sh
\`\`\`
</code></pre><h2 id="building-qgc-on-centos" tabindex="-1">Building QGC on CentOS <a class="header-anchor" href="#building-qgc-on-centos" aria-label="Permalink to &quot;Building QGC on CentOS&quot;">​</a></h2><h3 id="installing-qt" tabindex="-1">Installing Qt <a class="header-anchor" href="#installing-qt" aria-label="Permalink to &quot;Installing Qt&quot;">​</a></h3></li></ul><p>mkdir ~/devel cd ~/devel</p><pre><code>&lt;br /&gt;Install Qt 5.12.4 from the Qt installation script that can be downloaded [here](https://www.qt.io/download-thank-you?os=linux&amp;hsLang=en).
Once downloaded, make it executable and run it:
</code></pre><p>chmod +x qt-unified-linux-x64-3.1.1-online.run ./qt-unified-linux-x64-3.1.1-online.run</p><pre><code>&lt;br /&gt;Select the following options and install it under \`~/devel/Qt\`:

![Qt Software Selection](../../../assets/dev_getting_started/centos/qt_setup.png)

### Clone and Build QGC
</code></pre><p>git clone --recursive <a href="https://github.com/mavlink/qgroundcontrol.git" target="_blank" rel="noreferrer">https://github.com/mavlink/qgroundcontrol.git</a> mkdir build cd build</p><pre><code>For a debug/test build:
</code></pre><p>../Qt/5.12.4/gcc_64/bin/qmake ../qgroundcontrol/qgroundcontrol.pro -spec linux-g++ CONFIG+=debug</p><pre><code>For a release build:
</code></pre><p>../Qt/5.12.4/gcc_64/bin/qmake ../qgroundcontrol/qgroundcontrol.pro -spec linux-g++ CONFIG+=qtquickcompiler</p><pre><code>Build it:
</code></pre><p>make -j4 \`\`\`</p><p>You can alternatively launch <em>QtCreator</em> (found under <code>~/devel/Qt/Tools/QtCreator/bin/qtcreator</code>), load the <code>qgroundcontro.pro</code> project and build/debug from within its IDE.</p><p>By default, this will build the regular QGC. To build the sample, customized UI version, follow <a href="https://github.com/mavlink/qgroundcontrol/blob/master/custom-example/README.md" target="_blank" rel="noreferrer">these instructions</a>.</p>`,67),s=[i];function d(c,p,u,h,m,g){return r(),a("div",null,s)}const k=o(l,[["render",d]]);export{w as __pageData,k as default};
