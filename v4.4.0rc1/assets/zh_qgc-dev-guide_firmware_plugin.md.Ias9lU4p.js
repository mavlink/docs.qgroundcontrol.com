import{_ as e,o as i,c as t,R as a}from"./chunks/framework.DhMxou8O.js";const m=JSON.parse('{"title":"Plugin Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"zh/qgc-dev-guide/firmware_plugin.md","filePath":"zh/qgc-dev-guide/firmware_plugin.md"}'),r={name:"zh/qgc-dev-guide/firmware_plugin.md"},o=a('<h1 id="plugin-architecture" tabindex="-1">Plugin Architecture <a class="header-anchor" href="#plugin-architecture" aria-label="Permalink to &quot;Plugin Architecture&quot;">​</a></h1><p>虽然MAVLink规范定义了与载具通信的标准通信协议。 There are many aspects of that spec that are up for interpretation by the firmware developers. There are many aspects of that spec that are up for interpretation by the firmware developers. Because of this there are many cases where communication with a vehicle running one firmware is be slightly different than communication with a vehicle running a different firmware in order to accomplish the same task. Also each firmware may implement a different subset of the MAVLink command set.</p><p>另一个主要问题是MAVLink规范不包括载具配置或通用参数集。 Due to this all code which relates to vehicle setup ends up being firmware specific. Due to this all code which relates to vehicle setup ends up being firmware specific. 此外，任何必须引用特定参数的代码也是特定于固件的。</p><p>鉴于固件实现之间的所有这些差异，创建单个地面站应用程序可能非常棘手，可以支持每个应用程序而不会使代码库降级为基于车辆使用的固件在任何地方遍布的大量if / then / else语句。</p><p>QGC uses a plugin architecture to isolate the firmware specific code from the code which is generic to all firmwares. There are two main plugins which accomplish this <code>FirmwarePlugin</code> and <code>AutoPilotPlugin</code>.</p><p>This plugin architecture is also used by custom builds to allow ever further customization beyond when standard QGC can provide.</p><h2 id="firmwareplugin" tabindex="-1">FirmwarePlugin <a class="header-anchor" href="#firmwareplugin" aria-label="Permalink to &quot;FirmwarePlugin&quot;">​</a></h2><p>This is used to create a standard interface to parts of Mavlink which are generally not standardized.</p><h2 id="autopilotplugin" tabindex="-1">AutoPilotPlugin <a class="header-anchor" href="#autopilotplugin" aria-label="Permalink to &quot;AutoPilotPlugin&quot;">​</a></h2><p>This is used to provide the user interface for Vehicle Setup.</p><h2 id="qgccoreplugin" tabindex="-1">QGCCorePlugin <a class="header-anchor" href="#qgccoreplugin" aria-label="Permalink to &quot;QGCCorePlugin&quot;">​</a></h2><p>This is used to expose features of the QGC application itself which are not related to a Vehicle through a standard interface. This is then used by custom builds to adjust the QGC feature set to their needs.</p>',12),n=[o];function s(c,h,l,u,d,p){return i(),t("div",null,n)}const g=e(r,[["render",s]]);export{m as __pageData,g as default};
